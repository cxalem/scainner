# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust
target/

# Never commit real recorded car data — the app's DB lives outside the repo
# (~/Library/Application Support/com.cxalem.scainner/), but this is a defensive
# backstop in case a dev copies one in for local testing.
*.sqlite3
*.sqlite3-*

# Local-only heavy 3D assets for the dormant STL pipeline (29MB).
# The active scene uses public/models/ which IS committed.
public/models-preview/
